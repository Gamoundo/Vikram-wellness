import React from 'react'

function EditBtn(props) {
    // find something by id, replace it with new thing. need the whole, the part, the new part.
    // props need to be the entire array and the one being edited.
    // those can be passed down as props to make this component reusable.
    // also need to pass down the update state function.
    // would that mean the form is also created by the props?
    // loop through the object sent and make a form based on the values
    // send down the url?

    const handleEdit = () => {

    }

    const editForm = () => {

    }

    return(

        <div>
            <button> Edit</button>
        </div>
    )
}

export default EditBtn;